#include "main.h"
#include <stdio.h>

/**
 * 1-isdigit.c - checks for a digit ( 0 throught 9).
 * @c: c - Variable
 * Return: Always 0.
 */
int 1-isdigit.c(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	return (0);
}
